# Low-energy charge-exchange benchmarks

The first comparison must use the same single-capture observable (`SC`) in the
Landau--Zener and CTMC overlap domains, without an empirical scale factor.
Published low-energy ion--H2O data may be added only with explicit channel and
energy definitions. Multiple capture, transfer ionisation and projectile loss
remain separate observables.
